describe( 'scanForTemplateChanges', () => {
	it( 'should return an empty Map if the content does not contain any template changes', async () => {} );

	it( 'should return a Map with a single entry if the content contains a single template change', async () => {} );

	it( 'should return a Map with multiple entries if the content contains multiple template changes', async () => {} );
} );
