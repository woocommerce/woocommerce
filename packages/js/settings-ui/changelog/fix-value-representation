Significance: patch
Type: fix

Fix value handling for extension-supplied schemas: numeric option values match in select controls, the badge intent lookup ignores Object.prototype keys, and controls preserve the schema's value representation.
