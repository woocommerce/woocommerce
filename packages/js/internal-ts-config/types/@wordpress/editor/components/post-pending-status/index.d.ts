declare module '@wordpress/editor/build-types/components/post-pending-status' {
	/**
	 * A component for displaying and toggling the pending status of a post.
	 *
	 * @return {React.ReactNode} The rendered component.
	 */
	export function PostPendingStatus(): React.ReactNode;
	export default PostPendingStatus;
}
