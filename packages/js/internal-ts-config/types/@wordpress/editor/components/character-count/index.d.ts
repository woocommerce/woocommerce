declare module '@wordpress/editor/build-types/components/character-count' {
	/**
	 * Renders the character count of the post content.
	 *
	 * @return {number} The character count.
	 */
	export default function CharacterCount(): number;
}
