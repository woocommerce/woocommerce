declare module '@wordpress/editor/build-types/components/post-sync-status' {
	/**
	 * Renders the sync status of a post.
	 *
	 * @return {React.ReactNode} The rendered sync status component.
	 */
	export default function PostSyncStatus(): React.ReactNode;
}
