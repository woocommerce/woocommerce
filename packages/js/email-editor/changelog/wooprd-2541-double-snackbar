Significance: minor
Type: update

Rework snackbar notices by using the `SnackbarNotices` component from `@wordpress/notices` to render notices, removing the `email-editor` context snackbar notices renderer, and adding a `disableSnackbarNotices` setting to the email editor component
