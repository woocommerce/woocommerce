/**
 * External dependencies
 */
import { Spinner } from '@woocommerce/components';

export const Basic = () => (
	<div>
		<Spinner />
	</div>
);

export default {
	title: 'Components/Spinner',
	component: Spinner,
};
