Significance: patch
Type: changed

Relax the tk_ai visitor cookie from SameSite=Strict to Lax so it is sent on inbound cross-site navigations such as an ad click, and rewrite it on each page load with the same id so cookies set by earlier versions pick up the new attribute. The one-year expiry becomes rolling from the most recent visit.
