/**
 * External dependencies
 */
import { createSlotFill } from '@wordpress/components';

export const {
	Fill: ProductFilterSettingsControls,
	Slot: ProductFilterSettingsControlsSlot,
} = createSlotFill( 'ProductFilterSettingsControls' );

export const {
	Fill: ProductFilterStylesControls,
	Slot: ProductFilterStylesControlsSlot,
} = createSlotFill( 'ProductFilterStylesControls' );
