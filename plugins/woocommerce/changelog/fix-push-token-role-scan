Significance: patch
Type: performance

Push notifications: stop scanning every user's capabilities when fetching push tokens. get_tokens_for_roles() ran an unrestricted WP_User_Query role__in lookup (a wp_usermeta capabilities scan across all users, ~60s on very large sites) on every notification send; the user lookup is now restricted to the small set of users that actually own push tokens.
