Significance: patch
Type: fix

Generate the POS catalog feed in chunks across several Action Scheduler actions so large catalogs no longer exceed the host request timeout or Action Scheduler's failure period mid-generation; reduce the chunk size and restart from scratch when a run gets stuck; and raise the execution time and memory limits during generation.