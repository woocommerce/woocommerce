Significance: patch
Type: fix

Interpolate trusted analytics lookup/stats table names directly instead of using the `%i` placeholder in the Analytics order/product reports and the order milestone easter egg, so these queries stay valid on database layers that run on a supported WordPress version but don't implement `%i`.
