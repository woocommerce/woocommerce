Significance: patch
Type: dev
Comment: Adds the engine of the Back In Stock Notifications migration - run state, requirements, writer, reporter, batch processor and WP-CLI runner - along with the settings and product signup flag sections it can already carry over. Subscriber rows are not migrated and the Tools entry does not exist yet, so the migration is incomplete until the rest of the stack lands.
