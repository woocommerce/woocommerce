Significance: minor
Type: add
Comment: Experimental Add to Wishlist button block, gated by the `product_wishlist` feature flag.

Add the `woocommerce/add-to-wishlist-button` block: auto-injected after `woocommerce/add-to-cart-with-options` on the single-product template (block themes only) via the Block Hooks API. Toggles the currently configured product (parent or selected variation) in the shopper's wishlist via the shared `shopper-lists` iAPI store. Hidden for guests.
