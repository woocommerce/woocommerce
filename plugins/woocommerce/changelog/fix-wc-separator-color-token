Significance: patch
Type: fix

Introduce a --wc-separator-color CSS custom property for theme-aware divider colors. Migrate the universal-border-light SASS token, the WC login/register form border, the mini-cart footer border, and remove the rounded corners from the checkout order summary box so they all share the same separator color and shape.
