Significance: patch
Type: fix

Fix address fields reverting to defaults when country field is hidden via woocommerce_get_country_locale filter. When country is hidden, its value is now set to the store base country instead of being cleared, preserving locale resolution for all other fields. Also fixes state and phone fields not being hidden in classic checkout, and hides shipping calculator toggle when all fields are hidden.
