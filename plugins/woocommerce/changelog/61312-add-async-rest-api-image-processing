Significance: minor
Type: add

Add opt-in `images_async` (products, categories) and `image_async` (variations) REST API parameters to process product, variation, and category images asynchronously via Action Scheduler.
