Significance: minor
Type: add

Add is_standalone_line readonly boolean to the Store API cart-item response so clients can distinguish plain standalone lines from meta-differentiated lines (e.g. bundle children, bookings, add-ons) without exposing raw cart_item_data. True for a plain standalone line; false for a meta-differentiated line.
