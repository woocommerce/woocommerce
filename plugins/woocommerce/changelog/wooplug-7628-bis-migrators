Significance: patch
Type: dev
Comment: Adds the four section migrators that read Back In Stock Notifications data and write the Core equivalents, plus the run reporter. Nothing here registers a hook, so it stays unreachable until the migration is enabled.
