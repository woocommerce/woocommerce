Significance: minor
Type: dev

Remove Legacy REST API admin notices, auto-install logic, and UI nudges from WooCommerce core. The Legacy REST API settings section is now hidden by default and only renders when an extension hooks into the `woocommerce_settings_rest_api` filter. The Legacy REST API extension continues to work if manually installed.