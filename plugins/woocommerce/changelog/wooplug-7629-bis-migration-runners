Significance: patch
Type: dev
Comment: Adds the batch processor, WP-CLI runner and Tools entry that drive the Back In Stock Notifications migration. The command is not registered yet, and the processor only runs once enqueued, so none of it is reachable until the migration is enabled.
