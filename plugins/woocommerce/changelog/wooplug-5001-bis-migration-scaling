Significance: patch
Type: dev
Comment: Scales the Back In Stock Notifications migration to production-sized stores: the notifications section now walks the legacy table by keyset and decides candidacy in PHP from batched lookups, adoption resolves a whole batch in two queries instead of one per row, and cursors persist between runs. Nothing here is reachable until the migration is enabled.
