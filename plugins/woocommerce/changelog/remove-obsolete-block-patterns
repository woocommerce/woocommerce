Significance: major
Type: update

Remove 20 block patterns from the bundled pattern library. Existing inserted blocks are unchanged; themes and extensions referencing removed pattern slugs must update those references.
