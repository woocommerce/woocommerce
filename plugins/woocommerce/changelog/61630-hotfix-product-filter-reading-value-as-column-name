Significance: patch
Type: fix

Fixes malformed SQL query to obtain product counts associated with an attribute when using a custom attribute as a Product Filter, causing the filters to not appear on the frontend and silently fail.