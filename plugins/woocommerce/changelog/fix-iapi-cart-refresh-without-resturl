Significance: patch
Type: fix

Only enqueue the Mini-Cart view script module when the block is interactive, so it no longer loads on the cart and checkout pages, and prevent the iAPI cart store from firing a malformed cart fetch retry loop when its interactivity state is not registered.
