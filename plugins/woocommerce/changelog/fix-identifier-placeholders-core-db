Significance: patch
Type: fix

Interpolate trusted table and column identifiers directly instead of using the `%i` placeholder in the meta-to-meta table migrator, custom meta data store, and tax settings blueprint exporter, so these queries stay valid on database layers that run on a supported WordPress version but don't implement `%i`.
