Significance: minor
Type: fix

Store API: the order endpoint now returns item_data as a list, matching the cart endpoint and its own documented schema. The order item meta ID moves into a new `id` property.
