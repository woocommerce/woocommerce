Significance: minor
Type: add

Allow additional checkout field rules to reference other field values with `$data`, and compare `date` field values as numbers so rules can order one date against another.
