Significance: minor
Type: update

Remove the experimental abandoned cart recovery feature: the recovery email and its templates, the order-edit manual send action, the settings-page recommendations, the email unsubscribe endpoint, and the `abandoned_cart_recovery` feature flag. Leftover scheduled sends, order meta, options and unsubscribe records are cleaned up on update.
