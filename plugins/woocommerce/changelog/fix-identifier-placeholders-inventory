Significance: patch
Type: fix

Interpolate trusted table names directly instead of using the `%i` placeholder in the product attributes lookup and stock notifications data stores, so these queries stay valid on database layers that run on a supported WordPress version but don't implement `%i`.
