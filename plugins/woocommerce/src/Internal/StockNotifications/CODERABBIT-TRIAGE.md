# BIS Alpha — CodeRabbit Triage

> **Pre-GA scratchpad.** This doc is intentionally shipped inside `src/Internal/` to keep triage state close to the code it audits during the alpha sprint. It's listed in the SPRINT.md pre-GA checklist for deletion before the feature ships unconditionally — do not treat it as reference documentation.

Source: PR #59947 (merged 2025-08-20). 52 CodeRabbit line comments.
Generated: 2026-04-22 via automated triage against current trunk (branch `feature/back-in-stock-notifications`).

All paths are relative to the repo root. Each checkbox links to the originating CodeRabbit comment. Counts: 9 + 12 + 16 + 2 + 13 = **52**.

## Must-fix (9)

Correctness / fatal-error / data-loss / user-visibly-wrong. Do not ship a wider rollout without these.

- [x] `plugins/woocommerce/src/Internal/StockNotifications/Admin/NotificationEditPage.php:80` — Cancel action calls `set_date_notified( NotificationCancellationSource::ADMIN )` — copy-paste bug; should be `set_cancellation_source(...)`. An enum string is also written into a timestamp column. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629720))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Admin/NotificationEditPage.php:107-111` — The `send_verification_email` switch case adds a "Verification email sent to ..." success notice without actually dispatching any email. Deferred — overlaps with RSM-438 verification-email wiring. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629723))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Notification.php:486-525` — Both `get_verification_key()` (L488) and `get_unsubscribe_key()` (L518) write to the same `email_link_action_key` meta, so generating one silently invalidates the other. Use distinct meta keys. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632105))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Config.php:91` — Filter name `woocommerce_customer_stock_notifications_supported_product_stock_statuses` has a spurious `_stock` (the method is about product statuses, not stock statuses). Rename while still in alpha to avoid locking in a typo. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629740))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/AsyncTasks/NotificationsProcessor.php:171` — `$product_id = (int) $product_id ?? 0;` — cast binds tighter than `??`, so `?? 0` is dead. Replace with `isset($product_id) ? (int) $product_id : 0`. Currently logs `Background process for product 0 terminated` whenever the upstream `$product_id` is unset. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629735))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/DataRetentionController.php:86-89` — `Factory::get_notification()` can return `false`; `$notification->delete()` is then called unconditionally → fatal on the daily cron when any id is unreadable. Guard with `if ( $notification instanceof Notification )`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629742))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Admin/Templates/html-admin-notification-create.php:139` — `data-display_stock="true"data-placeholder="..."` — missing space between attributes merges them into one malformed attribute; select2 never sees `data-placeholder`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629728))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Admin/Templates/html-admin-notification-create.php:141` — `<option ...><?php ... ?><option>` — opening tag used instead of `</option>`. Invalid HTML inside `<select>`. (Line 103 has the same bug in the customer dropdown; not flagged separately by CodeRabbit but should be fixed in the same pass.) ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629729))
- [x] `plugins/woocommerce/templates/emails/customer-stock-notification-verified.php:58` — `sprintf( __( 'You have received this message because…' ), $product->get_name() )` — translation string has no `%s` placeholder, so the product name is silently dropped. Either add `%s` or stop passing the extra argument. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629818))

## Should-fix (12)

Real bugs with narrower blast radius, plus alpha-window-only API decisions (e.g. min-WP dep) and tests that pass for the wrong reasons.

- [ ] `plugins/woocommerce/src/Internal/DataStores/StockNotifications/StockNotificationsDataStore.php:280-298` — `update()` writes `date_created_gmt` on every save, re-deriving it from the object's current state. Once a notification is created, the creation date should be immutable. Drop it from the update payload. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632065))
- [ ] `plugins/woocommerce/src/Internal/DataStores/StockNotifications/StockNotificationsDataStore.php:450-452` (also 465-467, 475-482) — `query()` mixes `esc_sql()` on values that are subsequently fed through `$wpdb->prepare()`. Drop the `esc_sql()` — `prepare()` does the binding. Not a live security hole because `prepare()` still safely binds, but the double-handling will bite when someone later trusts that the value was already quoted. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632072))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Privacy/PrivacyEraser.php:49-83` — Same `Factory::get_notification()`-can-return-false pattern as `DataRetentionController` (fatal on any unreadable id). Also mixes `current_time('mysql')` for `set_date_cancelled()` while every other caller passes an integer `time()` — the setter then re-parses that as a timestamp-like value. Also no `is_email()` guard and no batching for large erase requests. *Null guard and `time()` normalisation addressed; `is_email()` guard and batching still outstanding.* ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629785))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Emails/EmailActionController.php:150-162` — Signature is `: ?Notification` but `return false` is used twice. Should be `return null`. PHP would coerce, but strict static analyzers and the `is_null` vs `! $x` branch in callers diverge. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629750))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Admin/Templates/html-admin-notification-edit.php:169` — Hardcoded `'active'` string compared against `$notification->get_status()` instead of `NotificationStatus::ACTIVE`. Since the notification-status enum is defined in this PR and may still move, this kind of drift is the whole reason to use the enum. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629734))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Admin/Templates/html-product-data-admin.php:14-21` — Template calls `$product->get_image_id()` / `get_availability()` with no null-safety; if the product is null or deleted, fatal. Add an `is_a($product, 'WC_Product')` guard at top. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632086))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Notification.php:66-69` — Constructor branch `is_array( $read ) && ! empty( $read['id'] )` calls `set_props($read)` + `set_object_read(true)` but never calls `set_id()`. An array-constructed Notification will `get_id() === 0`, which then trips every subsequent identity check. Add `$this->set_id( absint( $read['id'] ) );` before `set_props`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632097))
- [x] `plugins/woocommerce/src/Internal/StockNotifications/Utilities/HasherHelper.php:23-30` — Fallback `wp_fast_hash` uses `sodium_crypto_generichash( $key, 'wp_fast_hash_6.8+', 30 )` which diverges from WP 6.8 core's exact parameters. The whole helper is a pre-WP-6.8 compat shim; the right call before GA is either (a) bump WooCommerce's min-WP to 6.8 and delete the helper, or (b) align the fallback byte-for-byte with core so older sites produce hashes compatible with a future WP upgrade. *Shim removed — WC now requires WP 6.8.* ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629799))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Factory.php:45-62` — `create_dummy_notification()` creates an unsaved `WC_Product`, then `set_product_id( $product->get_id() )` — but unsaved products have id 0, so the notification is created with `product_id = 0`. The preview path works by accident via `$notification->product = $product`. Fix the dummy to use a sentinel id and stop setting public properties on `Notification`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629769))
- [ ] `plugins/woocommerce/tests/php/src/Internal/StockNotifications/AsyncTasks/CycleStateServiceTests.php:86-91` — Assertion on line 90 is unreachable (the call on line 89 throws, as declared by `expectException` on line 88). Test passes for the wrong reason; remove the dead assertion. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629840))
- [ ] `plugins/woocommerce/tests/php/src/Internal/StockNotifications/StockSyncControllerTests.php:71-78` — `add_action( ..., 100, 3 )` with a 1-arg callback. Harmless in PHP (extra args ignored) but the test asserts on a signature that's narrower than declared; reduce the arity argument to `1`, or expand the callback. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629852))
- [ ] `plugins/woocommerce/client/legacy/js/frontend/back-in-stock-form.js:96-106` — Attribute-name selector built via string concat — needs `$.escapeSelector()` to tolerate attribute names with brackets, quotes, or periods. Flagged as Should-fix rather than Must-fix because attribute slugs on a default WC install are always safe — only custom product attributes with special characters would trip it. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629706))

## Nice-to-have (16)

Polish, style, defensive-coding. No user-visible impact if shipped as-is.

- [ ] `plugins/woocommerce/client/legacy/css/admin.scss:9409-9414` — Hard-coded `width: 80% !important` on `.notification-data--create` select2 + OR-input; switch to fluid sizing with optional max-width. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629696))
- [ ] `plugins/woocommerce/src/Internal/DataStores/StockNotifications/StockNotificationsMetaDataStore.php:53-64` — `delete_by_notification_id()` has no type hint on `$notification_id`, no `: bool` return, no early-return guard for `<= 0`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629711))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Admin/NotificationCreatePage.php:27-119` — `process_create_form()` is ~90 lines mixing validation / user-resolution / dup-check / save. Split for testability. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629714))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Admin/Templates/html-admin-notification-create.php:120-136` — Admin-only template — CodeRabbit wants `current_user_can()` plus product-type eligibility guards before showing the selected product. Largely redundant because the menu is cap-gated and `wc_get_product()` only returns loaded products, but cheap to add. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629727))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Emails/EmailManager.php:65-71` — `email_classes()` instantiates three email objects on every filter fire. Cache on the instance. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629755))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Emails/EmailManager.php:103-128` — `maybe_restore_customer_tax_location_data()` registers an anonymous closure on `woocommerce_get_tax_location` that can't be removed once attached. Magic-number `4 !== count( $location )` could use a named shape. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629759))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Emails/EmailTemplatesController.php:101-111` — Variation-list `<dl>→<table>` conversion via `strtr()` is brittle. Fine for WC-controlled markup but not for 3PD filters on `wc_get_formatted_variation()`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629766))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/NotificationQuery.php:17-19` — `get_notifications()` is a thin passthrough without arg validation. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629774))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/NotificationQuery.php:27-29` — `product_has_active_notifications()` passthrough without arg validation. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629776))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/NotificationQuery.php:38-40` — `notification_exists_by_email()` passthrough without arg validation. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629777))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/NotificationQuery.php:49-51` — `notification_exists_by_user_id()` passthrough without arg validation. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629781))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/StockNotifications.php:70-77` — `register_data_stores()` does not try/catch the container lookup. In practice the container throws for missing services and that's fine. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629791))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/StockSyncController.php:170` — `$product_id` goes through `%d` inside `sprintf()`; CodeRabbit wants an explicit `absint()`. `%d` already coerces, purely cosmetic. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629796))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Utilities/HasherHelper.php:39-49` — `wp_verify_fast_hash` fallback — add empty-input guard + catch `SodiumException`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629804))
- [ ] `plugins/woocommerce/src/Internal/StockNotifications/Utilities/StockManagementHelper.php:46-54` — `$wpdb->get_col()` can return `null` on DB error; `array_map('intval', null)` throws on PHP 8+. Coerce to `array`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629806))
- [ ] `plugins/woocommerce/client/legacy/js/admin/wc-customer-stock-notifications.js:9-40` — Four near-identical confirm-before-delete handlers; extract a helper and guard `wc_admin_customer_stock_notifications_params` existence. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632055))

## Already fixed (2)

Verified against current code — the issue CodeRabbit flagged no longer exists.

- `plugins/woocommerce/src/Internal/StockNotifications/Admin/ListTable.php:481` — CodeRabbit claimed an unclosed `<option>` in the product-filter dropdown (lines 479-483). Current code at line 481 correctly uses `</option>`. Resolved at some point after the review; only the `html-admin-notification-create.php` copies of this pattern still exhibit the bug. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632077))
- `plugins/woocommerce/src/Internal/StockNotifications/Admin/ListTable.php:513` — Same claim for the customer-filter dropdown (lines 511-515). Current code at line 513 correctly uses `</option>`. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632081))

## Noise (13)

Not actionable — false-positives, design decisions explicitly justified by the author, test-coverage-wish prompts, or out-of-scope.

- `plugins/woocommerce/src/Internal/StockNotifications/Emails/CustomerStockNotificationEmail.php:86` — CodeRabbit flagged a call to an "undefined" `get_option_or_transient()`. The method is defined on the parent class `WC_Email` at `plugins/woocommerce/includes/emails/class-wc-email.php:1661`. False positive. (The handoff brief listed this as must-fix; it is a false alarm.) ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629747))
- `plugins/woocommerce/src/Internal/StockNotifications/Utilities/HasherHelper.php:28-29` — `_💡 Verification agent_` marker, empty body. Not actionable. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629800))
- `plugins/woocommerce/src/Internal/StockNotifications/Utilities/StockManagementHelper.php:46-50` — CodeRabbit called this "Critical: SQL injection". The query already builds `$format = array_fill( 0, count( $children ), '%d' )` and passes `$children` through `$wpdb->prepare()`, which binds each value as an integer. CodeRabbit's suggested `array_map('intval', $children)` is belt-and-braces but the vulnerability does not exist. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629811))
- `plugins/woocommerce/templates/emails/customer-stock-notification-verify.php:37` — Wants `$intro_content ?? ''` defensive fallback. The template is only invoked through `wc_get_template()` calls that always pass the variable. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629821))
- `plugins/woocommerce/templates/emails/customer-stock-notification-verify.php:55-62` — Same style of defensive `?? ''` fallback request. Not actionable. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629830))
- `plugins/woocommerce/tests/php/src/Internal/DataStores/StockNotifications/StockNotificationsDataStoreTests.php:35-41` — Claims "typo: `wc_stock_notificationmeta` should be `wc_stock_notificationsmeta`". The actual table the data store creates is `wc_stock_notificationmeta` (singular-meta), confirmed at `StockNotificationsMetaDataStore.php:26`. The test matches reality. False positive. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629835))
- `plugins/woocommerce/tests/php/src/Internal/StockNotifications/Emails/EmailActionControllerTests.php:22-36` — "Expand test coverage for verification action." Suggestion to add more tests, not a bug. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629845))
- `plugins/woocommerce/tests/php/src/Internal/StockNotifications/Emails/EmailActionControllerTests.php:41-56` — "Add test for non-existent notification id." Same pattern — write-more-tests prompt, not a bug. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629847))
- `plugins/woocommerce/tests/php/src/Internal/StockNotifications/Utilities/StockManagementHelperTests.php:30-37` — Same table-name "typo" claim as the DataStore tests. False positive. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231629855))
- `plugins/woocommerce/src/Internal/DataStores/StockNotifications/StockNotificationsDataStore.php:94` (email field length) — CodeRabbit wanted `varchar(100)` bumped to 320 for RFC 5321 compliance. The PR author argued that matching `wp_users.user_email` (100 chars) is the WP-core-consistent choice, and CodeRabbit's later reply (comment #51) accepts the explanation. Explicitly out of scope. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632060))
- `plugins/woocommerce/src/Internal/StockNotifications/Notification.php:28-33` — Wants `public $product` → `protected $product`. The property is documented as a runtime cache and `Factory::create_dummy_notification()` assigns it directly for preview-mode. Switching to `protected` would require adding a setter purely for the preview path; worth revisiting if that path is refactored (see the Factory item in Should-fix), but not actionable on its own. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632091))
- `plugins/woocommerce/src/Internal/StockNotifications/Notification.php:464-466` — Claims `str_contains` needs a PHP 7.x fallback. WooCommerce bundles the Symfony `polyfill-php80` package (at `plugins/woocommerce/bin/composer/mozart/vendor/symfony/polyfill-php80/`) and the function is widely used across the codebase. False positive. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2231632101))
- `plugins/woocommerce/src/Internal/DataStores/StockNotifications/StockNotificationsDataStore.php:90-107` — Author-thanks reply from CodeRabbit on the email-field-length thread (the "learnings added" message). Not a review comment. ([comment](https://github.com/woocommerce/woocommerce/pull/59947#discussion_r2243834275))

---

## Reconciliation with handoff brief

Of the 12 items flagged in the handoff as "already identified must-fixes", this triage keeps:

- **9** as Must-fix: NotificationEditPage:80, NotificationEditPage:107, Notification:488/518/525 (shared meta key), Config:91, NotificationsProcessor:171, DataRetentionController:87, html-admin-notification-create:139/141 (the 103 occurrence is not separately flagged by CodeRabbit but is the same bug), customer-stock-notification-verified:58.
- **2** downgraded: HasherHelper fallback (moved to Should-fix — the compat shim works; the "right" fix is a min-WP decision) and CustomerStockNotificationVerifyEmail:179 `siteurl` vs `home_url()` (moved to Should-fix, treated here under the verify-email template / EmailActionController group — CodeRabbit did not directly comment on that line).
- **1** recategorised: CustomerStockNotificationEmail:86 `get_option_or_transient()` → **Noise**. The handoff listed this as must-fix but the method is defined on the parent `WC_Email`; it is a CodeRabbit false-positive.

## Uncertainty notes

Every one of the 52 comments was verified against the current code at the line range it pointed to, so no entry above is a "can't-verify" placeholder. Two entries are worth calling out explicitly:

- **HasherHelper (Should-fix).** Shippability depends on the min-WP bump decision (pre-WP-6.8 compat shim), which is a team call rather than a code call.
- **PrivacyEraser `current_time('mysql')` vs `time()` (Should-fix).** CodeRabbit didn't flag this directly; it surfaced while verifying their error-handling comment. Filed under the same entry for convenience rather than as a separate row.
