<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	bootstrap="tests/legacy/bootstrap.php"
	backupGlobals="false"
	colors="true"
	convertErrorsToExceptions="true"
	convertNoticesToExceptions="true"
	convertWarningsToExceptions="true"
	verbose="true"
	defaultTestSuite="wc-phpunit-legacy,wc-phpunit-main,wc-phpunit-graphql"
	xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.3/phpunit.xsd">
	<testsuites>
		<!-- Granular suites referenced by CI `shardingArguments`. `legacy`
		     and `main` run as separate parallel shards; the GraphQL suite
		     rides along inside the `main` shard on PHP 8.1+ jobs. It stays
		     a separate suite purely because its tests need PHP 8.1+ (the
		     dummy fixture API and the public command tree use enums, named
		     arguments, etc.): the PHP 7.4 / 8.0 jobs point `shardingArguments`
		     at `legacy` + `main` only and so never discover — and therefore
		     never parse-fatal on — the GraphQL files. -->
		<testsuite name="wc-phpunit-legacy">
			<directory suffix=".php">./tests/legacy/unit-tests</directory>
		</testsuite>
		<testsuite name="wc-phpunit-main">
			<directory suffix=".php">./tests/php</directory>
			<exclude>./tests/php/helpers</exclude>
			<exclude>./tests/php/src/Blocks/BlockPatterns/patterns</exclude>
			<!-- Email-template fixtures are plain PHP that emit markup at
			     include-time (top-level HTML and `echo`). PHPUnit's directory
			     discovery `require`s every matched `.php` file, so leaving them
			     in would print their output to stdout during suite construction. -->
			<exclude>./tests/php/src/Internal/EmailEditor/WCTransactionalEmails/fixtures</exclude>
			<!-- Enumerated rather than a blanket ./tests/php/src/Api, so a new
			     ./tests/php/src/Api/<X>/ subdir is never silently picked up by
			     this suite. A new GraphQL test subdir must be added to the
			     wc-phpunit-graphql testsuite AND excluded here. -->
			<exclude>./tests/php/src/Api/Infrastructure</exclude>
			<exclude>./tests/php/src/Api/Queries</exclude>
			<exclude>./tests/php/src/Api/Mutations</exclude>
			<exclude>./tests/php/src/Internal/Api</exclude>
		</testsuite>
		<!-- GraphQL tests: the manually-maintained infrastructure under
		     src/Internal/Api/ and src/Api/Infrastructure/ (plus the resolver
		     tree generated by ApiBuilder against the dummy fixture API), and
		     the public command classes under src/Api/Queries/ and
		     src/Api/Mutations/ (products, coupons, …). One suite: the
		     infra/api split bought only CI shard parallelism, which no longer
		     applies now that GraphQL rides in the `main` shard. -->
		<testsuite name="wc-phpunit-graphql">
			<directory suffix=".php">./tests/php/src/Api/Infrastructure</directory>
			<directory suffix=".php">./tests/php/src/Internal/Api</directory>
			<directory suffix=".php">./tests/php/src/Api/Queries</directory>
			<directory suffix=".php">./tests/php/src/Api/Mutations</directory>
		</testsuite>
	</testsuites>
	<listeners>
		<listener class="JohnKary\PHPUnit\Listener\SpeedTrapListener">
			<arguments>
                <array>
					<element key="slowThreshold">
						<integer>1000</integer>
					</element>
					<element key="reportLength">
                        <integer>25</integer>
                    </element>
                </array>
            </arguments>
		</listener>
	</listeners>
	<coverage includeUncoveredFiles="true">
		<include>
			<directory suffix=".php">./includes</directory>
			<directory suffix=".php">./src</directory>
			<file>woocommerce.php</file>
			<file>uninstall.php</file>
		</include>
		<exclude>
			<directory suffix=".php">./includes/admin/helper/views</directory>
			<directory suffix=".php">./includes/admin/importers/views</directory>
			<directory suffix=".php">./includes/admin/meta-boxes/views</directory>
			<directory suffix=".php">./includes/admin/plugin-updates/views</directory>
			<directory suffix=".php">./includes/admin/views</directory>
			<directory suffix=".php">./includes/api/legacy</directory>
			<directory suffix=".php">./includes/api/v1</directory>
			<directory suffix=".php">./includes/legacy</directory>
			<directory suffix=".php">./includes/libraries</directory>
			<directory suffix=".php">./includes/shipping/legacy-flat-rate</directory>
			<directory suffix=".php">./includes/shipping/legacy-free-shipping</directory>
			<directory suffix=".php">./includes/shipping/legacy-international-delivery</directory>
			<directory suffix=".php">./includes/shipping/legacy-local-delivery</directory>
			<directory suffix=".php">./includes/shipping/legacy-local-pickup</directory>
			<directory suffix=".php">./includes/updates</directory>
			<directory suffix=".php">./includes/vendor</directory>
			<directory suffix=".php">./includes/widgets</directory>
			<directory suffix=".php">./packages</directory>
			<directory suffix=".php">./vendor</directory>
			<file>./includes/wc-deprecated-functions.php</file>
			<file>./includes/wc-template-hooks.php</file>
			<file>./includes/wc-widget-functions.php</file>
		</exclude>
	</coverage>
</phpunit>
