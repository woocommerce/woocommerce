/**
 * Side-effect entry that boots the iAPI bindings for the cart button.
 *
 * The actual store actions (moveToCartFromList, addToCartFromList) live on
 * the shared `woocommerce` namespace and will be wired in the iAPI store
 * follow-up. This file exists so the block has a script module entry today.
 */
export {};
