/*
 * Order of colors in the array affects the order of CSS variables and classes
 * in the generated CSS.
 * Changing the order will break the markup generated by the block,
 * producing validation errors.
 */
export const colorNames = [ 'sliderHandle', 'sliderHandleBorder', 'slider' ];
