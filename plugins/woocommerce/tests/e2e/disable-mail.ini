; Disable mail sending to match wp-env behavior for E2E tests
; This makes PHP's mail() function fail, which causes wp_mail() to return false
; Tests expecting email failures will now work correctly
sendmail_path=/bin/false