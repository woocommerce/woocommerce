const { getAppRoot, getAppName } = require( './app-root' );
const { getTestConfig, getAdminConfig } = require( './test-config' );

module.exports = {
	getAppRoot,
	getAppName,
	getTestConfig,
	getAdminConfig,
};
