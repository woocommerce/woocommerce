filter:
  excluded_paths:
    - tests/*
    - apigen/*
    - includes/api/v1/*
    - includes/libraries/*
    - includes/updates/*
    - includes/gateways/simplify-commerce/includes/*

checks:
  php:
    code_rating: true
    duplication: true
